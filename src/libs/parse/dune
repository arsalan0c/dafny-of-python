(menhir
  (modules parser)
  (flags ("--dump" "--explain")))
(ocamllex lexer)
(library
  (name pyparse)
  (libraries
    core
    sexplib
  )
  (preprocess (pps ppx_jane -allow-unannotated-ignores))
)
