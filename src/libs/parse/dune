(menhir
  (modules parser)
  (flags ("--dump" "--explain")))
(ocamllex lexer)
(library
  (name pyparse)
  (libraries
    core
    sexplib
    re2
  )
  (preprocess (pps ppx_jane -allow-unannotated-ignores))
  (flags (:standard -w -14))
)
